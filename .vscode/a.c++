#include <string>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int n,k,ml,l,g,r,x,y,z;
int main(){
    using namespace std;
    array<int,std::size_t(3)>shots={x,z,y};
    cin>>n;
    cin>>k;
    cin>>ml;
    cin>>l;
    cin>>g;
    x=(ml*k)/n;
    l=r*8;
    y=g;
    shots[(x,z,y)/n];

    sort(shots.begin(),shots.end());
    cout<<shots[0];}
